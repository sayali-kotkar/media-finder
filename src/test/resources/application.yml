itune:
  search: https://itunes.apple.com/search

google:
  books:
    search: https://www.googleapis.com/books/v1/volumes

hystrix:
 command:
  default:
   execution:
    timeout:
     enabled: false
 
  HysterixCommandKey:
    execution:
     timeout:
      enabled: false
